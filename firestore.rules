rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Dev-first rules: require authentication.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
